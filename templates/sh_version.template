Value version (.+?)
Value hostname (\S+)
Value uptime (.+)
Value model (\w+)

Start
  ^.*Software\s.+\),\sVersion\s${version},*\s+RELEASE.*
  ^\s*${hostname}\s+uptime\s+is\s+${uptime}
  ^Cisco\s+${model}\s+
